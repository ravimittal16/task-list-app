import { ErrorWordDirective } from './error-word.directive';

describe('ErrorWordDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorWordDirective();
    expect(directive).toBeTruthy();
  });
});
